# mcfish bench node signature: the full default position list at depth 13,
# Threads 1, Hash 16, and a SINGLE ucinewgame -- the table and the history
# block carry across positions. Every one of those four facts changes the
# number; see src/shell/benchmark.c. Requires a net: a fallback-eval run
# produces an unrelated total.
# Regenerate ONLY for an intended behaviour change, and say what moved it in
# the commit body. Updating this on a red gate launders a bug into the anchor.
2792255
