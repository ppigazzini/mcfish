# Build output
build/
*.o
*.s
*.d

# Editor / OS
.cache/
compile_commands.json
*.swp
.DS_Store

# Python bits used by the upstream test mirrors
__pycache__/
*.pyc
.venv/

# Files generated by the instrumented tests
tsan.supp
tests/syzygy
tests/bench_tmp.epd

# Neural network files (ccfish does not use one today; keep the pattern so a
# stray download from an upstream script is never committed)
**/*.nnue
